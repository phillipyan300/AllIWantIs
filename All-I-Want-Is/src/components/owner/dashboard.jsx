// Landing page will be default one user, then customized (probably customized via props?)

function Dashboard() {
    return (
        <div>
            <h1>Dashboard</h1>
        </div>
    );
}

export default Dashboard;